{
  "compilerOptions": {
    // 컴파일링 될 파일들의 JS버전을 정의
    "target": "ESNext",
    // 컴파일링 될 파일들의 모듈시스템을 정의
    // "module": "ESNext",
    // 컴파일링 될 파일들을 저장할 공간들을 정의할 수 있음
    // "outDir": "dist",
    // 컴파일링 하고자 하는, 파일의 타입을 엄격하게 검사 여부
    // "strict": true,
    "module": "ESNext",
    // 타입 지정 시, null 타입 사용에 대한 여부
    // useState(null) 이걸 사용할래 말래?
    "strictNullChecks": false,
    // 타입스크립트 각 파일을 독립적인 모듈로 인식
    "moduleDetection": "force",
    // 외부 라이브러리를 설치할 때,
    // 타입 지정 검사를 생략하고자 할 때,
    "skipLibCheck": true
  },
  "ts-node": {
    "esm": true
  },
  // src라는 폴더 안에 있는 파일들을 ts로 컴파일링 하고자 할 때,
  "include": ["src"]
}
