import { DefaultTheme } from "styled-components";

export const defaultTheme: DefaultTheme = {
  bgColor: "#2f3640",
  textColor: "#fff",
  accentColor: "#4cd137",
  btnColor: "transparent", // Use "transparent" as a string for color
  cardBgColor: "#333", // Add a default color for card background
};
