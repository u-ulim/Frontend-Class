let arr = [10, 5, 1, 2, 3, 4, 6, 7, 8, 9];

const selectionSort = (arr) => {
  for (let i = 0; i < arr.length - 1; i++) {}
};
console.log("=== 배열 정렬 전 ===");
console.log(arr);

selectionSort(arr);

console.log("=== 배열 정렬 후 ===");
console.log(arr);
